{
  "name": "birdcar/laravel-label-tree",
  "description": "A Laravel package for managing hierarchical labels as a DAG with materialized path routes",
  "type": "library",
  "license": "MIT",
  "version": "0.1.0",
  "authors": [
    {
      "name": "Birdcar",
      "email": "devrel@birdcar.dev"
    }
  ],
  "require": {
    "php": "^8.3",
    "illuminate/contracts": "^11.0|^12.0",
    "illuminate/database": "^11.0|^12.0",
    "illuminate/support": "^11.0|^12.0"
  },
  "require-dev": {
    "larastan/larastan": "^3.0",
    "laravel/pint": "^1.18",
    "orchestra/testbench": "^9.0|^10.0",
    "pestphp/pest": "^3.0",
    "phpstan/phpstan": "^2.0"
  },
  "autoload": {
    "psr-4": {
      "Birdcar\\LabelTree\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "Birdcar\\LabelTree\\Tests\\": "tests/"
    }
  },
  "extra": {
    "laravel": {
      "providers": [
        "Birdcar\\LabelTree\\LabelTreeServiceProvider"
      ]
    }
  },
  "config": {
    "sort-packages": true,
    "allow-plugins": {
      "pestphp/pest-plugin": true
    }
  },
  "minimum-stability": "stable",
  "prefer-stable": true,
  "scripts": {
    "post-autoload-dump": [
      "@clear",
      "@prepare"
    ],
    "clear": "@php vendor/bin/testbench package:purge-skeleton --ansi",
    "prepare": "@php vendor/bin/testbench package:discover --ansi",
    "lint": [
      "@php vendor/bin/pint --ansi",
      "@php vendor/bin/phpstan analyse --verbose --ansi"
    ],
    "test": [
      "@clear",
      "@php vendor/bin/pest --compact"
    ],
    "test:sqlite": "DB_CONNECTION=testing vendor/bin/pest --compact",
    "test:mysql": "DB_CONNECTION=mysql vendor/bin/pest --compact",
    "test:pgsql": "DB_CONNECTION=pgsql vendor/bin/pest --compact",
    "test:all": [
      "@test:sqlite",
      "@test:mysql",
      "@test:pgsql"
    ],
    "benchmark": "@php vendor/bin/pest --group=benchmark --compact",
    "benchmark:scaling": "@php vendor/bin/pest --group=scaling --compact",
    "benchmark:docs": "@php scripts/generate-benchmark-docs.php"
  }
}
